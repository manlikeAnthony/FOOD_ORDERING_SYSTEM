apiVersion: v1
kind: Service
metadata:
  name: food-ordering-service
spec:
  type: NodePort
  selector:
    app: food-ordering-app
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30007